export type PersonalInfo = {
    fullName: string
    email: string
    phoneNumber: string
}

export type BusinessInfo = {
    name: string
    email: string
    phoneNumber: string
    address: string
    postalCode: string
    city: string
    facebook: string
    twitter: string
    instagram: string
    websiteUrl:string
}